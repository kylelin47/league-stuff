from setuptools import find_packages, setup

setup(
    name='riotapi',
    version='0.1',
    install_requires=[
        'requests >=2.7.0',
    ],
    packages=find_packages(),
    scripts=[
    ],
)
